log:
  level: "info"

runner:
  file: "C:\\actions-runner\\.runner"
  capacity: 1
  timeout: "3h"
  insecure: false
  fetch_interval: "2s"
  labels:
    - "windows:host"
    - "windows-latest:host"
    - "windows-node:host"
    - "windows-2022:host"

cache:
  enabled: true
  dir: "C:\\tmp\\cache"

host:
  workdir_parent: "C:\\tmp\\act"
